{
  "name": "conjure-typescript-runtime",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna run publish",
    "lint": "lerna run --no-bail lint",
    "test": "lerna run --no-bail test",
    "verify": "lerna run verify"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:munkyshi/conjure-typescript-runtime.git"
  },
  "author": "Palantir Technologies, Inc",
  "license": "Apache-2.0"
}
